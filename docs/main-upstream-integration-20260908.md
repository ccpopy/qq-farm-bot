# main 吸收已同步上游的 master（2026-09-08）

本次以 `main` 为目标分支，选择性适配 `master` 中的上游改动。上游为
`liyangpengs/qq-farm-bot`，核对时最新提交是 `304dcdf`。

`master` 的上游同步提交为 `8af22c6`。误将 main 功能加到 master 的 `d3960dc`
已通过 `8c1ee31` 撤销，保留上游同步和原有 TSDK 适配，未改写已发布历史。

## 本次吸收的内容

| 来源 | main 中的适配 |
| --- | --- |
| `2d384fd` 多季作物修复 | 按服务端阶段后缀与作物配置识别成熟状态；收获后仍无法判定的土地跳过铲除；适配到 main 独立的施肥模块，并将普通、有机、智能补肥限制在目标土地及所选土地类型内。 |
| `304dcdf` 前台优先 | 后台农场/好友业务最多一个在途请求；有前台排队时暂停新后台请求；防饥饿调度不越过前台；心跳、ACE 独立通道继续保留。 |
| `304dcdf` 互动道具读取 | 好友土地道具和农场道具复用一次背包与活动上下文读取，先背包后活动，保留 main 的好友访问会话和活动列表缓存。邮箱双列表在 main 已串行，补入上游回归测试。 |
| `4ee6894`、`5ddb70b` QQ 扫码 | 增加持久化登录设置、后端代理、二维码轮询、授权 Code 和取消接口。补上弹窗关闭后迟到二维码的取消、重建前等待注销、创建请求的超时设置。QQ 扫码默认关闭，需另行配置 qq-miniapp-auth 服务。 |
| `8da9a5a` 好友页读取 | 访客记录、GID 设置和互动道具按需加载，避免未运行账号与重复进入触发读取，保留已选道具。好友列表仍读取本地黑名单以显示屏蔽标记。 |
| `cc6a8ab` 错误与背包修复 | 保留服务端业务错误 message/code，兼容 main 的 mutation trace 和不确定结果处理；果实为 type=6、超变果实为 type=17；背包支持使用化肥道具。 |
| `228f1b9` 推送 | 吸收 MeoW 离线提醒渠道与昵称配置。 |
| `e44cc12` 会员领取 | 非 QQ 会员结果按当天跳过处理，避免反复领取和错误日志。 |

项目版本更新为 `20260908`，默认游戏协议更新为 `1.13.3.17_20260826`。

## 保留 main 的实现

- 统一账号任务队列、启动顺序、前台读取、好友分片巡查与完整访问会话继续使用 main 的模块，不引入 master 的第二套自动化锁。
- 持久 API Token、背包 UID 栈处理、操作结果核对、好友短缓存及跨活动列表缓存继续保留。
- 活动中心沿用 main 的模块拆分、公益协议验证、领取状态与确认界面，不用 master 的活动页面和协议定义整体覆盖。
- TSDK 与 WASM 保留 main 已有的平台适配；Android 开发不在本次范围内。

## 验证

在独立 main 工作树完成后端构建，219 项后端回归和 5 项前端单元测试全部通过；
前端类型检查、生产构建和所有改动源码的 ESLint 检查通过。
新增回归覆盖多季作物、补肥范围、前台优先、QQ 登录代理、互动库存复用、协议错误、MeoW 和背包分类。
页面检查使用静态构建与模拟 API，不连接真实游戏账号或发送实际推送。
已验证二维码创建期间取消、正常取消、重开时不重复创建任务，访客记录按需读取及重复切换去重，
普通/超变果实分类和化肥使用数量弹窗。QQ 扫码尚未进行外部服务的真人扫码联调。
生产构建仍有现存的 `carbon-renew-1` 图标缺失提示。
